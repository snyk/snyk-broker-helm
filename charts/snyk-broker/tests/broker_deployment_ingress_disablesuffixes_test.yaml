suite: test broker deployment with ingress (No suffixes)
chart:
  version: 0.0.0
templates:
  - broker_deployment.yaml
  - broker_service.yaml
  - secrets.yaml
  - serviceaccount.yaml
  - broker_ingress.yaml
values:
  - ./fixtures/default_values.yaml
  - ./fixtures/disable_suffixes.yaml
  - ./fixtures/enable_ingress.yaml

tests:
  - it: ingress
    asserts:
      - matchSnapshot: {}
