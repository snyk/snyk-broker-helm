suite: test broker deployment (credential pooling)
chart:
  version: 0.0.0
templates:
  - broker_deployment.yaml
  - broker_service.yaml
  - secrets.yaml
  - serviceaccount.yaml
values:
  - ./fixtures/default_values.yaml

tests:
  - it: github token pool configured
    values:
      - ./fixtures/github_tokenpool.yaml
    asserts:
      - matchSnapshot: {}
  - it: gitlab token pool configured
    values:
      - ./fixtures/gitlab_tokenpool.yaml
    asserts:
      - matchSnapshot: {}
  - it: github token pool configured with enabled useExternalSecretScmTokenPool
    values:
      - ./fixtures/github_tokenpool_external_secret.yaml
    asserts:
      - matchSnapshot: {}
